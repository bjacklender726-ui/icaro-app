services:
  - type: static_site
    name: icaro-app
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /:path*
        destination: /index.html
